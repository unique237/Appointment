{
  "name": "example1",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.3.1",
    "cors": "^2.8.5",
    "twilio": "^5.3.5"
  },
  "devDependencies": {
    "twilio-run": "^4.0.3"
  },
  "engines": {
    "node": "18"
  }
}
